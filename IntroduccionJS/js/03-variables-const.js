// Variables con const
// No se pueden reasignar, no se pueden redeclarar, no se pueden inicializar sin valor.
// Constantes son inmutables, no pueden cambiar su valor una vez asignado. Sin embargo, si el valor es un objeto o un array, sus propiedades o elementos pueden ser modificados.

const producto = 'Audifonos Gamer'; // Iniciamos la variable producto y le asignamos un valor.

const disponible = true; // Iniciamos la variable disponible con un valor.

