// Objeto Math

let resultado;

resultado = Math.PI; // Valor de pi 3.141592653589793
resultado = Math.round(2.5); // Redondea el número al entero más cercano (2.5 se redondea a 3)
resultado = Math.ceil(2.1); // Redondea el número hacia arriba (2.1 se redondea a 3)
resultado = Math.floor(2.9); // Redondea el número hacia abajo (2.9 se redondea a 2)
resultado = Math.sqrt(16); // Raíz cuadrada de un número (16 se convierte en 4)
resultado = Math.abs(-5); // Valor absoluto de un número (-5 se convierte en 5)
resultado = Math.pow(2, 3); // Potencia de un número (2 elevado a la 3 se convierte en 8)
resultado = Math.max(10, 20, 5); // Devuelve el número máximo de una lista de números (10, 20 y 5 devuelve 20)
resultado = Math.min(10, 20, 5); // Devuelve el número mínimo de una lista de números (10, 20 y 5 devuelve 5)
resultado = Math.random(); // Devuelve un número aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 100); // Devuelve un número aleatorio entre 0 y 99
resultado = Math.floor(Math.random() * 100) + 1; // Devuelve un número aleatorio entre 1 y 100

console.log(resultado); // Imprime el resultado